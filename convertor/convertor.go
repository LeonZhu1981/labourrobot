package convertor

import (
	//. "github.com/ahmetalpbalkan/go-linq"
	"labourrobot/model"
)

func Convert(metaDataList []model.TaxInsuranceMetaData) (taxInsurance model.TaxInsurance, err error) {

	return model.TaxInsurance{}, nil
}
